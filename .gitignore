# Claude Code state - the laptop's global ~/.claude config is used instead
.claude/
CLAUDE.local.md
.claude-plans/
*.plan.md

node_modules/
dist/
.env
.env.*
!.env.example

# Mined artwork - ~900 full-size PNGs do not belong in git.
# assets/manifest.json is tracked and makes these reproducible.
assets/pokemon/
assets/items/
assets/thumbs/

npm-debug.log*
.DS_Store
Thumbs.db
