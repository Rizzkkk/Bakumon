# API smoke test

Generated by `npm run smoke` (`scripts/smoke-api.js`) on 2026-09-23.
**Do not hand-edit.**

73 of 73 checks passed against `http://localhost:3001`.

The rate-limit check runs last because it spends the reference bucket for the
remainder of the window. Re-running inside the same minute needs the window to roll
over, or an API restart.

| Check | Result | Expected |
|---|---|---|
| health leaks no configuration beyond its status | pass | `["status"]` |
| health reports ok against a reachable database | pass | `"ok"` |
| pokemon total is the species count, duplicates included (ADR 0006) | pass | `904` |
| pokemon list defaults to page 1, pageSize 24, and fills the page | pass | `{"page":1,"pageSize":24,"rows":24}` |
| the last pokemon page is partial, not padded or truncated | pass | `{"rows":16,"total":904}` |
| a page past the end returns no rows but still a truthful total | pass | `{"rows":0,"total":904}` |
| pokemon browse ordering is unchanged by relevance ranking | pass | `["abomasnow","abra","absol"]` |
| a four-character search uses the vector alone, without substring noise | pass | `["abra"]` |
| relevance ranking on pokemon promotes prefix matches without dropping rows | pass | `{"total":9,"slugs":["litleo","litten","litwick","blitzle","flittle","gigalith","growlithe","politoed","vanillite"]}` |
| a two-character search falls back to substring matching | pass | `true` |
| a bare % is escaped rather than matching every row | pass | `0` |
| the abra card carries a computed spawn summary, not a stub | pass | `{"buckets":["common","rare","uncommon"],"biomeCount":4,"formCount":1}` |
| abra resolves with its identity fields intact | pass | `{"slug":"abra","displayName":"Abra","nationalDexId":63}` |
| the detail response ships exactly the fields the contract lists | pass | `["displayName","id","imageUrl","nationalDexId","slug","spawns"]` |
| abra has three spawns with the right buckets, weights and levels | pass | `[["common",1.8,6,31,"grounded"],["uncommon",7.5,6,31,"grounded"],["rare",8.4,6,31,"grounded"]]` |
| aerodactyl kept both weights, and NUMERIC came back as numbers | pass | `[10,0.1]` |
| arbok reaches the API as one pokemon carrying its form aspects, not seven | pass | `{"rows":8,"patterns":7}` |
| arbok detail spawns lead with the base form | pass | `{}` |
| a missing pokemon returns the 404 body the contract specifies | pass | `{"status":404,"body":{"error":"Pokemon not found"}}` |
| every one of the five buckets filters to a non-empty result | pass | `{"common":true,"legendary event":true,"rare":true,"ultra-rare":true,"uncommon":true}` |
| a bucket filter actually constrains the rows it returns | pass | `true` |
| an unknown bucket is rejected rather than silently returning nothing | pass | `{"status":400,"body":{"error":"Unknown bucket: mythic"}}` |
| an unknown biome token is rejected against the published closed list | pass | `{"status":400,"body":{"error":"Unknown biome: minecraft:not_a_biome"}}` |
| a two-value bucket list returns the union of species, not the species with both | pass | `733` |
| a bucket and a biome filter intersect across dimensions rather than union | pass | `235` |
| an unknown member of a bucket list is named specifically, not the whole list | pass | `{"status":400,"body":{"error":"Unknown bucket: mythic"}}` |
| a biome list past the 20-member cap is rejected before any member is checked | pass | `{"status":400,"body":{"error":"Too many biome values: max 20"}}` |
| a single bucket value still returns exactly the total it always did | pass | `545` |
| a repeated bucket parameter is rejected exactly as before, not read as a list | pass | `{"status":400,"body":{"error":"bucket must be given once"}}` |
| magikarp appears when filtered on a bucket it actually has (uncommon) | pass | `["magikarp"]` |
| and disappears when filtered on a bucket it lacks (rare), so the filter really filters | pass | `[]` |
| magikarp appears via a bucket list when it has any one member (uncommon), despite lacking rare | pass | `["magikarp"]` |
| the biome list is the closed set the filter is built from | pass | `{"tokens":112,"tags":68,"concrete":44,"references":16982}` |
| list and detail responses are cacheable by an edge layer | pass | `{"/api/pokemon":"public, max-age=60","/api/items":"public, max-age=60","/api/pokemon/abra":"public, max-age=60","/api/items/cobblemon:ability_capsule":"public, max-age=60"}` |
| the biome vocabulary is cached for an hour, not a minute | pass | `"public, max-age=3600"` |
| the cacheable biomes response varies on Origin | pass | `true` |
| item total is the listable rows: 934 imported, less the printf-named row | pass | `933` |
| the items browse page does not lead with a printf format string | pass | `[]` |
| browse ordering is unchanged by relevance ranking | pass | `["cobblemon:poke_puff","cobblemon:ability_capsule","cobblemon:ability_patch"]` |
| the API serves a placeholder item name exactly as the workbook records it | pass | `"%s Poke Puff"` |
| and it leads page one, where the excluded row used to sort | pass | `"%s Poke Puff"` |
| berries, fossils and poke balls filter on the source axis (ADR 0002) | pass | `{"berries-berry-items":84,"fossils":16,"poke-balls-catching":99}` |
| the four canonical categories reconcile to the listable item total | pass | `{"consumable":216,"evolution":74,"held":45,"other":598}` |
| the canonical categories sum to 933 | pass | `933` |
| berry is rejected on the canonical axis, where it does not live | pass | `{"status":400,"body":{"error":"Unknown category: berry"}}` |
| a stopword search falls back to substring instead of returning nothing | pass | `123` |
| an exact name match outranks alphabetical order | pass | `["Potion","Hyper Potion","Max Potion","Super Potion"]` |
| a prefix match outranks a mid-string match | pass | `"Poke Cake"` |
| ranking changes the order of results, never the count | pass | `{"over":123,"poke":127,"potion":4}` |
| a searched page past the end still returns a truthful total, not a 500 | pass | `{"status":200,"rows":0,"total":4}` |
| and the same holds for the pokemon list | pass | `{"status":200,"rows":0,"total":9}` |
| a bare % is escaped in the prefix rank as well as the predicate | pass | `{"total":4,"names":["%s Poke Puff","Deluxe %s Poke Puff","Fancy %s Poke Puff","Frosted %s Poke Puff"]}` |
| search hides the printf-named localisation row | pass | `[]` |
| but a direct link to that row still resolves | pass | `{"status":200,"itemId":"cobblemon:aprijuice.quality_format"}` |
| an item with no description on either axis reports null, not a false credit | pass | `{"category":"held","description":null,"descriptionSource":null}` |
| descriptionSource names the column the text actually came from | pass | `["workbook","wiki"]` |
| the items list carries the evolution-use count the index column renders | pass | `[["cobblemon:leaf_stone",10],["cobblemon:leaf_stone_block",0]]` |
| the free-text evolution uses stay off the list row, where nothing reads them | pass | `["category","description","descriptionSource","evolutionUseCount","id","imageUrl","itemId","name","sourceCategory"]` |
| the detail row still carries both the count and the free text | pass | `{"count":10,"hasText":true}` |
| a missing item returns the 404 body the contract specifies | pass | `{"status":404,"body":{"error":"Item not found"}}` |
| an unrouted path under /api returns JSON, not an HTML error page | pass | `{"status":404,"body":{"error":"Not found"}}` |
| a malformed percent-escape is a client error, not a 500 | pass | `{"status":400,"body":{"error":"Bad request"}}` |
| a mistyped parameter is named rather than silently ignored | pass | `{"status":400,"body":{"error":"Unknown query parameter: pagesize"}}` |
| an unknown query parameter on the pokemon detail route is a 400, matching the lists | pass | `{"status":400,"body":{"error":"Unknown query parameter: foo"}}` |
| an unknown query parameter on the item detail route is a 400, matching the lists | pass | `{"status":400,"body":{"error":"Unknown query parameter: foo"}}` |
| an unknown query parameter on /api/biomes is a 400, matching the lists | pass | `{"status":400,"body":{"error":"Unknown query parameter: foo"}}` |
| pageSize is bounded | pass | `{"status":400,"body":{"error":"pageSize must be an integer between 1 and 100"}}` |
| an enormous page number is rejected rather than overflowing bigint | pass | `{"status":400,"body":{"error":"page must be an integer between 1 and 1000000"}}` |
| a non-decimal page is rejected rather than silently serving page 16 | pass | `400` |
| a repeated parameter is rejected rather than coerced | pass | `{"status":400,"body":{"error":"page must be given once"}}` |
| CORS fails closed for an unknown origin without erroring the request | pass | `{"status":200,"allowOrigin":null}` |
| the server does not advertise what it runs | pass | `null` |
| an exhausted rate limit returns JSON, not the library default HTML | pass | `{"status":429,"contentType":"application/json","body":{"error":"Too many requests"}}` |
