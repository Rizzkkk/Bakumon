// Empty until real server screenshots land in assets/screenshots/ (pre-production item 7).
// The grid hides itself while this is empty rather than rendering placeholder tiles: a
// landing page with six grey boxes says "unfinished" louder than no gallery at all.
//
// To populate: drop files in assets/screenshots/, list them here with alt text that
// describes what the screenshot actually shows, and they appear. Alt text is hand-written
// for these because there are six to eight of them and each shows something different -
// unlike the 1,635 artwork files, which are generated from display names.
export const SCREENSHOTS = [];
